<!DOCTYPE html>
<html>
  <head>
    <title>Todo</title>
    <%= csrf_meta_tags %>
    <link href='http://fonts.googleapis.com/css?family=Lato:300,400,700' rel='stylesheet' type='text/css'>
    <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css" rel="stylesheet">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

  </head>
  <body>
  
       <% if !user_signed_in? %>
       <div class="container">
       <p id="notice"><%= notice %></p>
       <%= yield %>
       </div>
       <% end %>

       <% if user_signed_in? %>
       <%= link_to "Log Out", destroy_user_session_path, method: :delete, style: "color:black" %><br />
       <div class="container">
      <font color="purple"><i class="fa fa-cog fa-spin fa-2x fa-fw" ></i><strong><%= link_to current_user.email, users_show_path %></strong></font><br>
      <p id="notice"><%= notice %></p>
      <%= yield %>
       </div>
       <% end %>

  </body>
</html>

